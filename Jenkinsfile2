pipeline{
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages{
        stage ('maven  all commands'){
            steps{
                sh 'mvn clean test compile install package'
            }
        }
    }
}